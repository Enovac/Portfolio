# Local-only environment variables (do not commit .env)
# Vite exposes variables prefixed with VITE_ to the client at build time.

# Example placeholder (only needed if you add code that uses it):
# VITE_PUBLIC_BUILDER_KEY=your_public_key_here

# Example used by the old dev API (can be removed if unused):
# PING_MESSAGE="ping pong"
